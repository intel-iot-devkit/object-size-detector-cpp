graph TD
A[C++ code] -->B[OpenCV]
B-->V[OpenVINO backend]
B -->O[OpenCV backend]
V-->D[CPU]
V-->E[GPU 32-bit]
V-->F[GPU 16-bit]
V-->G[VPU]

graph TD
A[C++] -->B[OpenCV]
B-->Caffe
B -->TensorFlow
B -->Torch
B -->Darknet
B-->V(OpenVINO IR)

graph TD
subgraph cloud
S[MQTT Server]
end
subgraph local
subgraph Frame thread
Q[Take next available frame from queue]-->P[Look assembly parts using OpenCV]
P-->U[Notify if defect found]
end
subgraph Main thread
C[Capture frames]-->MQ[Add to queue]
MQ-->W[Window with display including stats]
end
subgraph Messaging thread
M[Send MQTT messages to server with stats]-- optional -->S
end
end
